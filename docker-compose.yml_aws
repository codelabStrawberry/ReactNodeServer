version: "3.8"

services:
  node:
    build: ./server
    env_file:
      - ./server/.env
    expose:
      - "3000"
    networks:
      - backend

  nginx:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - node
    networks:
      - backend

networks:
  backend:
    driver: bridge
